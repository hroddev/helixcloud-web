---
import ServicePage from "../../components/ServicePage.astro";
import { siteData } from "../../siteData";

const service = siteData.servicePages.find(
	(s) => s.slug === "migracion-hibrida",
);
---

<ServicePage service={service} />
